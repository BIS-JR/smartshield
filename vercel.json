{
  "framework": "vite",
  "buildCommand": "pnpm --filter @smartshield/shared build && pnpm --filter web build && rm -rf dist && cp -r apps/web/dist dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile"
}
